请设计并开发一款面向 macOS 的现代桌面浏览器。

名字是Rex

浏览器底层使用 Chromium 内核，原生界面使用 Swift、SwiftUI，并在必要时结合 AppKit 实现。产品可以研究并吸收 Arc 浏览器的垂直标签页、工作空间和分屏浏览理念，以及 Brave 浏览器的隐私保护能力，但不得直接复制其商标、品牌素材、图标、文案、界面布局或专有实现。

产品核心方向：

* 使用 Chromium 提供网页渲染与现代 Web 平台能力。
* 使用 SwiftUI 构建原生 macOS 界面。
* 默认采用垂直标签页。
* 支持工作空间和标签页分组。
* 提供类似 Arc 思路的双页面分屏浏览。
* 提供类似 Brave 思路的隐私保护功能。
* 整体 UI 使用液态玻璃设计语言。
* 不包含任何 AI 聊天、AI 总结、AI 搜索或 AI 自动操作功能。

⸻

一、产品定位

打造一款兼具高效标签管理、沉浸式分屏浏览、原生 macOS 体验和隐私保护能力的现代浏览器。

产品应重点解决以下问题：

1. 顶部横向标签页在标签数量较多时难以管理。
2. 用户在比较资料、编辑文档或查看参考内容时，需要频繁切换标签页。
3. 传统浏览器工具栏占用过多网页显示空间。
4. 用户无法直观了解网站的追踪、Cookie 和权限行为。
5. 普通 Chromium 浏览器在 macOS 上缺乏统一的原生视觉体验。

产品不应只是简单的 Chromium 套壳，而应围绕垂直标签页、空间管理、分屏浏览和隐私防护形成独立的产品体验。

⸻

二、产品设计原则

1. 内容优先

网页内容是视觉中心。

界面应减少传统浏览器顶部标签栏和厚重工具栏带来的空间占用，通过紧凑导航栏、可折叠侧边栏和沉浸式窗口，让网页获得更大的显示区域。

2. 垂直管理

标签页默认显示在左侧边栏。

垂直布局应支持：

* 更长的页面标题。
* 更多标签页同时显示。
* 标签页分组。
* 固定标签页。
* 工作空间。
* 拖拽排序。
* 快速搜索。
* 自动休眠和归档。

3. 分屏优先

分屏浏览不是附属功能，而是浏览器的核心能力之一。

用户应能轻松将两个网页放入同一窗口中进行：

* 对比。
* 阅读。
* 搜索。
* 编辑。
* 复制。
* 拖拽。
* 资料整理。
* 视频与内容并行查看。

4. 隐私默认开启

广告拦截、跨站追踪器拦截、第三方 Cookie 限制和 HTTPS 升级等基础隐私保护应默认开启。

用户应能清楚看到浏览器保护了什么，并能针对单个网站进行调整。

5. macOS 原生体验

浏览器应符合 macOS 的窗口、菜单、键盘、拖拽、触控板、分享、权限和无障碍规范。

6. 克制的液态玻璃

液态玻璃用于表达层级、焦点和空间关系，而不是覆盖整个页面。

玻璃效果必须兼顾：

* 可读性。
* 性能。
* 对比度。
* 电池续航。
* 无障碍需求。

⸻

三、技术架构

采用分层设计。

1. Chromium 内核层

Chromium 层负责：

* HTML、CSS 和 JavaScript 渲染。
* 多进程网页运行。
* 网络请求。
* Cookie。
* Local Storage。
* IndexedDB。
* Service Worker。
* WebRTC。
* 音视频播放。
* 网页下载。
* PDF 浏览。
* 网页打印。
* 页面查找。
* 页面缩放。
* 开发者工具。
* GPU 硬件加速。
* 网页权限请求。
* 网页进程崩溃隔离。
* 浏览器扩展支持。
* 网站隔离。
* Chromium 沙箱。

可评估以下实现方式：

* Chromium Embedded Framework。
* Chromium Content API。
* 自维护 Chromium 分支。
* 其他支持多进程、沙箱和 GPU 加速的 Chromium 嵌入方案。

必须明确：

* Chromium 版本。
* macOS 最低版本。
* Apple Silicon 支持。
* Intel Mac 支持范围。
* Chromium 编译流程。
* Swift 集成方案。
* 多进程架构。
* 浏览器沙箱能力。
* 浏览器更新策略。
* 扩展兼容范围。
* 安全补丁更新方式。
* 已知技术限制。

2. Swift 原生应用层

使用 Swift、SwiftUI，并在必要时结合 AppKit 实现：

* 主窗口。
* 垂直标签页。
* 工作空间。
* 地址栏。
* 导航栏。
* 分屏浏览器。
* 隐私盾牌。
* 下载管理器。
* 历史记录。
* 收藏夹。
* 标签页归档。
* 设置中心。
* 网站权限管理。
* 多窗口管理。
* 菜单栏。
* 右键菜单。
* 系统通知。
* 分享面板。
* 文件选择器。
* 拖拽操作。
* 触控板手势。
* 键盘快捷键。

3. Swift 与 Chromium 通信层

建立类型安全、异步、可测试的通信桥。

建议定义：

BrowserCommand
BrowserEvent
BrowserTab
BrowserSpace
TabGroup
SplitViewSession
SplitPane
NavigationState
PrivacyState
PrivacyReport
BlockedResource
WebsitePermission
DownloadTask
BridgeMessage

通信层至少支持：

* 创建网页实例。
* 销毁网页实例。
* 加载 URL。
* 后退。
* 前进。
* 刷新。
* 停止加载。
* 获取页面标题。
* 获取页面图标。
* 获取当前 URL。
* 获取加载进度。
* 获取音频播放状态。
* 获取页面安全状态。
* 获取网页权限请求。
* 获取隐私拦截结果。
* 调整网页缩放。
* 页面内查找。
* 打开开发者工具。
* 恢复崩溃页面。
* 将网页移动到分屏区域。
* 在分屏两侧交换网页。

不得将高权限 Swift 或系统接口直接暴露给网页 JavaScript。

⸻

四、主窗口布局

主窗口使用紧凑、沉浸式布局。

主要结构：

┌─────────────────────────────────────────────────────────┐
│ 窗口控制区  后退 前进  地址栏  隐私盾牌  更多操作        │
├───────────────┬──────────────────────┬──────────────────┤
│               │                      │                  │
│ 垂直标签侧边栏 │      主网页区域       │   第二网页区域    │
│               │                      │   可选分屏区域    │
│               │                      │                  │
├───────────────┴──────────────────────┴──────────────────┤
│ 可选状态栏、下载提示或页面信息                           │
└─────────────────────────────────────────────────────────┘

主窗口由以下区域构成：

1. 紧凑导航区域。
2. 液态玻璃垂直侧边栏。
3. 主网页内容区域。
4. 可选第二网页分屏区域。
5. 分屏调整控件。
6. 浮动隐私面板。
7. 下载或权限提示面板。

⸻

五、垂直标签页设计

参考 Arc 的垂直标签管理理念，但建立独立的视觉和交互体系。

1. 标签页基本信息

每个标签页显示：

* 网站图标。
* 页面标题。
* 加载状态。
* 音频状态。
* 固定状态。
* 休眠状态。
* 隐私拦截状态。
* 分屏状态。
* 页面崩溃状态。

2. 标签页操作

支持：

* 单击切换。
* 双击固定。
* 拖动排序。
* 多选。
* 批量关闭。
* 批量移动。
* 移动到其他工作空间。
* 添加到分组。
* 从分组移除。
* 固定标签页。
* 取消固定。
* 静音。
* 复制标签页。
* 休眠。
* 关闭。
* 恢复关闭标签页。
* 在新窗口中打开。
* 在分屏中打开。
* 与当前页面组成分屏。
* 从分屏中移除。

3. 标签页分区

侧边栏可划分为以下区域。

收藏区域

用于长期使用的网站和 Web 应用。

特点：

* 使用紧凑图标布局。
* 可跨工作空间共享。
* 支持拖拽排序。
* 支持通知角标。
* 支持快速启动。

固定标签区域

用于当前工作空间中长期保留的页面。

特点：

* 不自动归档。
* 支持自定义名称。
* 支持分组和文件夹。
* 支持手动排序。
* 支持折叠。

临时标签区域

用于普通浏览标签页。

特点：

* 按使用时间组织。
* 支持自动休眠。
* 支持自动归档。
* 支持快速清理。
* 支持重复标签检测。

4. 侧边栏状态

侧边栏支持：

* 完整展开。
* 紧凑图标模式。
* 自动隐藏。
* 手动折叠。
* 调整宽度。
* 鼠标靠近窗口边缘显示。
* 触控板手势呼出。
* 使用快捷键显示或隐藏。

侧边栏折叠后，网页区域应自动扩大。

⸻

六、工作空间设计

浏览器支持多个工作空间。

每个工作空间拥有独立的：

* 标签页。
* 固定标签。
* 标签分组。
* 主题色。
* 收藏配置。
* 隐私级别。
* 网站权限。
* Cookie 容器，可作为可选功能。
* 下载位置。
* 会话恢复状态。

工作空间示例：

* 工作。
* 个人。
* 学习。
* 开发。
* 娱乐。
* 临时研究。

支持：

* 创建工作空间。
* 删除工作空间。
* 重命名。
* 设置图标。
* 设置主题色。
* 快速切换。
* 拖动标签页到其他空间。
* 将分屏组合移动到其他空间。
* 恢复上一次打开的空间。

空间切换可以使用：

* 侧边栏按钮。
* 键盘快捷键。
* 触控板左右滑动。
* 菜单栏命令。

⸻

七、分屏浏览核心设计

分屏浏览参考 Arc 的 Split View 使用理念，但需要形成独立的交互逻辑和视觉体系。

1. 分屏模式

浏览器支持在同一窗口中同时显示两个网页。

支持：

* 左右分屏。
* 上下分屏。
* 主次分屏。
* 等比例分屏。
* 自定义比例分屏。

默认使用左右分屏。

建议初始比例：

* 50% / 50%。
* 60% / 40%。
* 70% / 30%。

用户可自由拖动分隔条调整比例。

2. 创建分屏的方式

用户可以通过以下方式进入分屏：

标签页拖拽

将一个标签页从侧边栏拖到当前网页区域的左侧、右侧、顶部或底部。

拖动过程中显示明确的分屏落点预览。

右键菜单

标签页右键菜单提供：

* 与当前标签页左右分屏。
* 与当前标签页上下分屏。
* 在左侧分屏打开。
* 在右侧分屏打开。

链接右键菜单

网页链接右键菜单提供：

* 在新标签页打开。
* 在左侧分屏打开。
* 在右侧分屏打开。

工具栏按钮

导航栏提供分屏按钮。

点击后可选择：

* 从现有标签页中选择。
* 新建空白页面。
* 打开剪贴板链接。
* 打开收藏页面。
* 打开最近关闭页面。

快捷键

建议支持：

* Command + Shift + S：创建或关闭分屏。
* Command + Option + Left：聚焦左侧页面。
* Command + Option + Right：聚焦右侧页面。
* Command + Shift + Left：将当前页面移动到左侧。
* Command + Shift + Right：将当前页面移动到右侧。

3. 分屏交互

分屏状态下，每个页面拥有独立的：

* Chromium 页面实例。
* URL。
* 页面标题。
* 导航历史。
* 加载进度。
* 页面缩放。
* 音频状态。
* 权限状态。
* 隐私状态。
* 页面查找状态。
* 开发者工具实例。

当前获得焦点的页面应有清晰但克制的视觉边界。

可以使用：

* 细微玻璃高光。
* 柔和外边缘。
* 低透明度描边。
* 地址栏页面状态变化。
* 分隔条方向指示。

4. 分屏导航

导航栏应根据当前焦点页面执行：

* 后退。
* 前进。
* 刷新。
* 停止。
* 地址输入。
* 收藏。
* 分享。
* 页面缩放。
* 隐私设置。

当用户切换焦点时，地址栏自动更新为当前页面的 URL。

也可为每个分屏页面显示紧凑的独立浮动地址栏。

5. 分屏页面操作

支持：

* 左右页面交换位置。
* 上下页面交换位置。
* 将左侧页面关闭。
* 将右侧页面关闭。
* 将某一侧恢复为普通标签页。
* 将分屏拆分为两个独立标签页。
* 将分屏拆分为两个独立窗口。
* 将分屏组合保存。
* 将分屏组合添加到收藏。
* 将分屏组合移动到其他工作空间。
* 复制其中一个页面。
* 重新加载单侧页面。
* 单独静音某一侧页面。
* 单独进入阅读模式。
* 单独打开开发者工具。

6. 分屏组合

可以将两个网页保存为一个分屏组合。

数据模型示例：

struct SplitViewSession: Identifiable, Codable {
    let id: UUID
    var primaryTabID: UUID
    var secondaryTabID: UUID
    var orientation: SplitOrientation
    var ratio: Double
    var focusedPane: SplitPane
    var spaceID: UUID
    var createdAt: Date
    var lastOpenedAt: Date
}

保存内容包括：

* 两个页面的 URL。
* 页面标题。
* 页面图标。
* 分屏方向。
* 分屏比例。
* 所属工作空间。
* 页面滚动位置。
* 最后激活页面。
* 页面缩放级别。

7. 分屏拖拽

支持在两个网页间拖动：

* 文本。
* 链接。
* 图片。
* 文件。
* 选中内容。

拖拽必须遵守网页安全规则。

不得因为分屏功能绕过：

* 浏览器沙箱。
* 同源策略。
* 本地文件访问限制。
* 网站权限。
* 剪贴板权限。

8. 分屏性能

两个页面同时显示时：

* 当前焦点页面获得较高资源优先级。
* 非焦点页面保持正常渲染，但可降低非必要定时器优先级。
* 播放视频或音频时不得自动冻结。
* 页面隐藏部分不应持续执行无意义重绘。
* 调整分隔条时避免频繁重建 Chromium 实例。
* 分屏比例变化应直接调整原生容器尺寸。
* 防止 SwiftUI 布局更新导致 Chromium 视图闪烁。
* 分屏切换动画不得阻塞网页主线程。

⸻

八、标签页归档和资源管理

1. 标签页状态

标签页可以处于：

* 活跃。
* 后台。
* 分屏活跃。
* 休眠。
* 冻结。
* 已归档。
* 已崩溃。

2. 自动归档

支持以下规则：

* 12 小时未使用。
* 24 小时未使用。
* 3 天未使用。
* 7 天未使用。
* 浏览器退出时归档。
* 从不归档。

归档应保留：

* URL。
* 页面标题。
* 页面图标。
* 所属工作空间。
* 所属标签分组。
* 最后访问时间。
* 页面历史。
* 页面滚动位置。
* 分屏组合关系。

3. 自动休眠

后台标签页在满足条件后可以释放 Chromium 页面资源。

以下标签不得自动休眠：

* 正在播放音频。
* 正在播放视频。
* 正在下载。
* 正在上传文件。
* 正在进行视频会议。
* 正在共享屏幕。
* 正在使用摄像头或麦克风。
* 当前处于分屏中的标签页。
* 用户手动设置为保持活跃的标签页。

⸻

九、Brave 风格隐私保护设计

参考 Brave 的隐私保护理念，但不复制其品牌名称、视觉和具体实现。

1. 默认保护功能

默认启用：

* 广告拦截。
* 跨站追踪器拦截。
* 第三方 Cookie 限制。
* HTTPS 自动升级。
* URL 追踪参数清理。
* 恶意网站检测。
* 钓鱼网站检测。
* 可疑下载提示。
* 自动播放控制。
* WebRTC IP 泄露防护。
* 基础浏览器指纹防护。
* 可疑重定向拦截。

2. 隐私盾牌

地址栏旁提供液态玻璃隐私盾牌按钮。

点击后显示当前网站的隐私报告：

* 已拦截广告数量。
* 已拦截追踪器数量。
* 已阻止第三方 Cookie 数量。
* 已升级 HTTPS 请求数量。
* 已清理 URL 参数数量。
* 已阻止的可疑脚本。
* 指纹防护状态。
* 网站当前拥有的权限。
* 当前网站保存的数据量。

用户可以在面板中：

* 为当前网站关闭保护。
* 调整拦截等级。
* 允许或禁止脚本。
* 管理 Cookie。
* 管理自动播放。
* 管理网站权限。
* 清除当前网站数据。
* 添加兼容性例外。
* 举报网页显示问题。

3. 隐私等级

提供：

标准模式

平衡网页兼容性和隐私保护。

严格模式

增强追踪器、Cookie、脚本和指纹保护，可能影响部分网页。

自定义模式

允许分别设置：

* 广告和追踪器。
* Cookie。
* JavaScript。
* 指纹防护。
* URL 参数清理。
* HTTPS 升级。
* 自动播放。
* 弹窗。
* 网站权限。

4. 指纹保护

针对以下信息提供限制、隔离或随机化策略：

* Canvas。
* WebGL。
* AudioContext。
* 字体列表。
* 屏幕尺寸。
* 颜色深度。
* 时区。
* 语言列表。
* CPU 核心数。
* 设备内存。
* 媒体设备信息。
* User-Agent 高熵字段。

需要优先考虑网页兼容性，并支持按网站添加例外。

5. 隐私窗口

隐私窗口应：

* 不写入普通浏览历史。
* 不保存搜索记录。
* 使用独立 Cookie 容器。
* 关闭后清理会话数据。
* 默认不恢复标签页。
* 默认不允许扩展运行。
* 使用更明显的窗口视觉状态。
* 支持分屏，但两侧页面必须都处于隐私会话中。
* 不允许普通窗口页面与隐私窗口页面组成分屏。

必须明确提示：隐私窗口无法对网络服务商、公司网络管理员或访问的网站隐藏用户的网络活动。

6. 网站权限中心

支持管理：

* 摄像头。
* 麦克风。
* 屏幕录制。
* 定位。
* 通知。
* 剪贴板。
* 文件访问。
* 自动下载。
* 弹窗。
* 自动播放。
* 蓝牙。
* USB。
* MIDI。

权限选项：

* 每次询问。
* 仅本次允许。
* 始终允许。
* 始终阻止。
* 标签页关闭后撤销。

⸻

十、液态玻璃 UI 设计

整体 UI 必须使用液态玻璃设计语言。

1. 视觉关键词

* 通透。
* 柔和。
* 流动。
* 轻盈。
* 沉浸。
* 分层。
* 原生。
* 高可读性。
* 克制。
* 精致。

2. 液态玻璃应用区域

液态玻璃主要用于：

* 垂直侧边栏。
* 地址栏。
* 导航按钮。
* 标签页选中状态。
* 工作空间切换器。
* 分屏分隔条。
* 分屏浮动工具栏。
* 隐私盾牌面板。
* 下载面板。
* 弹出菜单。
* 设置侧边栏。
* 权限提示。
* 右键菜单。
* 浮动状态提示。

网页内容区域本身不得添加大面积玻璃蒙层。

3. 玻璃材质表现

使用：

* macOS 系统材质。
* 半透明背景。
* 背景模糊。
* 低饱和度渐变。
* 柔和高光。
* 细微环境反射。
* 半透明边缘描边。
* 轻微噪点。
* 多层阴影。
* 窗口激活状态变化。
* 背景亮度自适应。

玻璃容器需要根据背景自动调整：

* 透明度。
* 模糊强度。
* 文字颜色。
* 描边亮度。
* 阴影强度。

4. 浅色模式

浅色模式建议：

* 乳白。
* 浅灰。
* 冰蓝。
* 柔和银色。
* 低饱和强调色。

避免使用过强的纯白高光和纯黑阴影。

5. 深色模式

深色模式建议：

* 石墨色。
* 蓝黑色。
* 深灰色。
* 低饱和蓝紫色。
* 冷白高光。

玻璃容器必须和背景保持足够对比度。

6. 标签页视觉

普通标签页：

* 透明度较低。
* 无强边框。
* 悬停时出现轻微玻璃高光。

当前标签页：

* 更高不透明度。
* 柔和内高光。
* 轻微描边。
* 网站图标和标题更清晰。

分屏标签页：

* 显示双页面组合标识。
* 使用两个网站图标。
* 显示当前焦点页面。
* 悬停时提供拆分按钮。

7. 分屏视觉

分屏分隔条使用液态玻璃设计。

默认状态：

* 较细。
* 低透明度。
* 不干扰网页。

悬停状态：

* 增加宽度。
* 提高亮度。
* 显示拖动指示。
* 显示交换页面按钮。
* 显示拆分按钮。

拖动状态：

* 显示实时比例。
* 使用流动高光。
* 页面边界清晰。
* 保持动画流畅。

8. 动画要求

需要设计以下动画：

* 侧边栏展开和收起。
* 工作空间切换。
* 标签页移动。
* 标签页固定。
* 标签页归档。
* 创建分屏。
* 移除分屏。
* 交换分屏页面。
* 拖动分隔条。
* 隐私盾牌展开。
* 弹出菜单出现。
* 按钮按压。
* 地址栏聚焦。

动画建议控制在 150 至 300 毫秒。

动画应：

* 使用自然弹性。
* 不延迟用户操作。
* 不阻塞 Chromium 渲染。
* 支持减少动态效果。
* 在低电量模式下降低复杂度。

⸻

十一、SwiftUI 液态玻璃组件

请创建统一的设计系统。

至少包含：

LiquidGlassBackground
LiquidGlassPanel
LiquidGlassSidebar
LiquidGlassToolbar
LiquidGlassButton
LiquidGlassIconButton
LiquidGlassTextField
LiquidGlassTabRow
LiquidGlassTabGroup
LiquidGlassSpaceSwitcher
LiquidGlassSplitContainer
LiquidGlassSplitDivider
LiquidGlassSplitToolbar
LiquidGlassPrivacyShield
LiquidGlassPopover
LiquidGlassContextMenu
LiquidGlassDownloadPanel
LiquidGlassProgressView
LiquidGlassPermissionPrompt

每个组件需要支持：

* 浅色模式。
* 深色模式。
* 系统强调色。
* 正常状态。
* 悬停状态。
* 按压状态。
* 聚焦状态。
* 选中状态。
* 禁用状态。
* 降低透明度。
* 增强对比度。
* 减少动态效果。
* 低电量模式。

⸻

十二、快捷键设计

至少支持：

* Command + T：新建标签页。
* Command + W：关闭当前页面。
* Command + Shift + T：恢复关闭的标签页。
* Command + L：聚焦地址栏。
* Command + R：刷新。
* Command + [：后退。
* Command + ]：前进。
* Control + Tab：切换标签页。
* Command + 1...9：切换常用标签页或工作空间。
* Command + Shift + S：创建或关闭分屏。
* Command + Option + Left：聚焦左侧页面。
* Command + Option + Right：聚焦右侧页面。
* Command + Shift + Left：当前页面移动到左侧。
* Command + Shift + Right：当前页面移动到右侧。
* Command + Shift + D：收藏页面。
* Command + Shift + Delete：清除浏览数据。
* Command + ,：打开设置。
* Escape：关闭浮动面板。

所有核心操作必须能够只使用键盘完成。

⸻

十三、性能要求

1. Chromium 性能

* 网页渲染进程与主应用进程分离。
* 页面崩溃不得导致整个浏览器退出。
* 支持 GPU 加速。
* 支持后台标签页冻结。
* 支持标签页休眠。
* 支持内存压力响应。
* 支持会话恢复。
* 支持单个崩溃页面恢复。
* 分屏页面不得重复创建不必要的浏览器进程。

2. SwiftUI 性能

* 不在主线程执行 Chromium IPC。
* 不在主线程读取大型缓存和历史记录。
* 标签页列表使用懒加载。
* 网站图标使用缓存。
* 限制大面积实时模糊。
* 避免全窗口持续重绘。
* 调整分屏比例时避免重建视图。
* Chromium 容器应稳定复用。
* 目标帧率为 60 FPS。
* 支持高刷新率显示器。
* 低电量模式下降低动画和玻璃效果。

3. 分屏性能

* 分屏两侧分别维护独立页面状态。
* 焦点页面获得更高资源优先级。
* 非焦点页面不应被错误休眠。
* 视频、音频和会议页面保持活跃。
* 拖动分隔条期间保持连续渲染。
* 不得因为调整布局导致页面闪白。
* 不得频繁销毁和创建 Chromium 实例。

⸻

十四、安全要求

必须实现：

* Chromium 沙箱。
* 网站隔离。
* 渲染进程隔离。
* IPC 消息验证。
* Swift Bridge 白名单。
* URL 协议验证。
* 文件下载安全检测。
* 扩展权限审查。
* macOS Keychain 敏感数据存储。
* Cookie 加密存储。
* 隐私规则签名验证。
* 权限最小化。
* 本地文件访问限制。
* 安全日志脱敏。
* 页面崩溃恢复。
* 内容拦截规则完整性校验。

不得通过分屏功能绕过：

* 同源策略。
* Cookie 隔离。
* 权限隔离。
* 隐私窗口隔离。
* 文件访问限制。
* 剪贴板权限。
* 摄像头和麦克风权限。

⸻

十五、核心数据模型

请设计以下模型：

struct BrowserTab
struct BrowserSpace
struct TabGroup
struct NavigationState
struct SplitViewSession
struct SplitPaneState
struct PrivacyState
struct PrivacyReport
struct BlockedResource
struct WebsitePermission
struct BrowserProfile
struct ArchivedTab
struct DownloadTask

BrowserTab 至少包含：

struct BrowserTab: Identifiable {
    let id: UUID
    var url: URL?
    var title: String
    var faviconURL: URL?
    var spaceID: UUID
    var groupID: UUID?
    var isPinned: Bool
    var isFavorite: Bool
    var isArchived: Bool
    var isSleeping: Bool
    var isPlayingAudio: Bool
    var isMuted: Bool
    var isLoading: Bool
    var loadingProgress: Double
    var privacyState: PrivacyState
    var splitSessionID: UUID?
    var lastAccessedAt: Date
}

SplitViewSession 至少包含：

struct SplitViewSession: Identifiable {
    let id: UUID
    var primaryTabID: UUID
    var secondaryTabID: UUID
    var orientation: SplitOrientation
    var ratio: Double
    var focusedPane: SplitPane
    var spaceID: UUID
    var createdAt: Date
    var lastOpenedAt: Date
}

⸻

十六、工程模块划分

建议工程结构：

Application
AppShell
WindowManagement
BrowserCore
ChromiumIntegration
BrowserBridge
Navigation
Tabs
TabGroups
Spaces
SplitView
Privacy
ContentBlocking
Permissions
Downloads
History
Bookmarks
Archive
Profiles
Settings
DesignSystem
Persistence
Security
Diagnostics
Tests

架构要求：

* 使用 Swift Concurrency。
* UI 状态由 MainActor 管理。
* Chromium 操作通过独立 Actor 或服务层执行。
* 使用协议隔离 Chromium 具体实现。
* 使用依赖注入。
* 避免全局可变状态。
* 避免大量单例。
* 核心操作支持撤销。
* IPC 支持结构化日志。
* 日志不得记录 Cookie、Token、密码或表单敏感内容。

⸻

十七、无障碍要求

支持：

* VoiceOver。
* 完整键盘导航。
* 动态字体。
* 增强对比度。
* 降低透明度。
* 减少动态效果。
* 色弱友好状态。
* 明确焦点指示。
* 图标辅助说明。
* 可调节侧边栏宽度。
* 可调节分屏分隔条。
* 网页缩放与界面缩放分离。

当系统启用“降低透明度”时：

* 液态玻璃背景切换为高可读性的实体或近实体背景。
* 保留原有视觉层级。
* 不使用透明度作为唯一状态提示。
* 分屏焦点使用描边、图标或文字共同表达。

⸻

十八、开发阶段

第一阶段：基础浏览器

实现：

* Chromium 集成。
* 页面加载。
* 基础导航。
* 垂直标签页。
* 多窗口。
* 下载管理。
* 历史记录。
* 收藏夹。
* 会话恢复。

第二阶段：标签与工作空间

实现：

* 工作空间。
* 固定标签页。
* 标签页分组。
* 自动休眠。
* 自动归档。
* 标签页搜索。
* 跨窗口移动标签页。

第三阶段：分屏浏览

实现：

* 左右分屏。
* 上下分屏。
* 拖拽创建分屏。
* 链接在分屏打开。
* 分屏比例调整。
* 页面焦点切换。
* 页面位置交换。
* 分屏组合保存。
* 分屏会话恢复。
* 分屏拖拽。
* 分屏性能优化。

第四阶段：隐私保护

实现：

* 广告拦截。
* 追踪器拦截。
* 第三方 Cookie 限制。
* HTTPS 升级。
* URL 参数清理。
* 指纹防护。
* 隐私盾牌。
* 权限中心。
* 隐私窗口。

第五阶段：液态玻璃与体验优化

实现：

* 完整液态玻璃设计系统。
* 标签动画。
* 分屏动画。
* 工作空间切换动画。
* 触控板手势。
* 无障碍适配。
* 性能优化。
* 低电量模式。
* 崩溃恢复。
* 网页兼容性测试。

⸻

十九、明确禁止的功能

产品不包含：

* AI 聊天助手。
* AI 网页总结。
* AI 搜索。
* AI 标签页整理。
* AI 地址栏。
* AI 自动操作。
* AI 页面内容读取。
* AI 写作。
* AI 翻译。
* AI 推荐。
* 云端模型调用。
* 本地大语言模型。

地址栏仅负责：

* 输入 URL。
* 使用搜索引擎搜索。
* 搜索历史记录。
* 搜索收藏夹。
* 搜索当前标签页。
* 执行明确的浏览器命令。

⸻

二十、最终交付内容

请输出：

1. 完整产品需求文档。
2. 功能地图。
3. 信息架构。
4. 主窗口布局。
5. 垂直标签页交互说明。
6. 工作空间设计。
7. 分屏浏览完整交互设计。
8. 分屏状态转换图。
9. 分屏数据模型。
10. 分屏会话恢复方案。
11. 隐私保护架构。
12. Chromium 与 Swift 技术架构图。
13. Swift 与 Chromium 通信时序图。
14. Swift 工程目录。
15. 核心协议定义。
16. 可运行的 SwiftUI 主窗口代码。
17. Chromium 容器封装代码。
18. 垂直标签页代码。
19. 工作空间切换代码。
20. 左右分屏代码。
21. 上下分屏代码。
22. 分屏拖拽代码。
23. 分屏分隔条代码。
24. 分屏页面交换代码。
25. 液态玻璃设计系统代码。
26. 液态玻璃侧边栏代码。
27. 液态玻璃隐私盾牌代码。
28. 内容拦截接口设计。
29. 权限管理设计。
30. 性能优化方案。
31. 安全方案。
32. 单元测试方案。
33. UI 自动化测试方案。
34. 分阶段开发计划。
35. 技术风险清单。
36. 最小可发布版本范围。

代码应尽可能完整、可运行、可扩展，不要只提供概念性伪代码。

对于依赖 Chromium 私有接口、自维护 Chromium 分支或系统特殊权限的功能，需要明确说明：

* 技术限制。
* 替代实现。
* 开发成本。
* 安全风险。
* Chromium 更新成本。
* 对 macOS App Sandbox 的影响。
* 对 Mac App Store 发布的影响。

二十一、版本功能列表与更新日志

项目必须建立统一、可查看、可追踪的版本功能日志系统。

每完成一个开发版本，都必须同步更新功能列表和版本日志。不得只在代码提交记录中描述改动，也不得依赖开发人员口头记录。

1. 日志目标

功能日志用于让开发者、测试人员和用户清楚了解：

* 当前版本已经实现哪些功能。
* 相比上一版本新增了哪些功能。
* 哪些功能得到优化。
* 哪些问题已经修复。
* 哪些功能发生变化。
* 哪些功能被移除或暂时关闭。
* 哪些功能仍在开发中。
* 当前版本存在哪些已知问题。
* 下一版本计划实现什么。

2. 日志存储位置

项目根目录中至少维护以下文件：

CHANGELOG.md
FEATURES.md
ROADMAP.md

各文件职责如下：

CHANGELOG.md

记录每个发布版本相对于上一版本发生的变化。

FEATURES.md

记录当前软件全部功能及其完成状态。

ROADMAP.md

记录未来版本计划、开发优先级和预计功能范围。

建议目录结构：

ProjectRoot/
├── CHANGELOG.md
├── FEATURES.md
├── ROADMAP.md
├── Documentation/
│   ├── Releases/
│   │   ├── v0.1.0.md
│   │   ├── v0.2.0.md
│   │   └── v1.0.0.md
│   └── FeatureSpecifications/
└── Sources/

每个正式版本还需要生成独立的版本详情文件：

Documentation/Releases/v版本号.md

3. 版本号规范

使用语义化版本号：

主版本号.次版本号.修订号

示例：

0.1.0
0.2.0
0.2.1
1.0.0

版本号含义：

* 主版本号：存在重大架构或不兼容变化。
* 次版本号：新增向后兼容的功能。
* 修订号：错误修复、性能优化或小型调整。

开发阶段可以使用：

0.1.0-alpha.1
0.1.0-beta.1
1.0.0-rc.1

4. 功能状态

FEATURES.md 中的每项功能必须标记状态。

统一使用以下状态：

✅ 已完成
🧪 测试中
🚧 开发中
📋 已规划
⏸ 已暂停
⚠️ 存在限制
❌ 已移除

每项功能至少包含：

* 功能名称。
* 所属模块。
* 当前状态。
* 首次加入版本。
* 最近修改版本。
* 功能说明。
* 已知限制。
* 相关设置入口。
* 是否需要重启。
* 是否支持隐私窗口。
* 是否支持分屏。
* 是否支持键盘操作。

5. 功能列表格式

FEATURES.md 使用以下格式：

# 功能列表
最后更新版本：v0.3.0
最后更新日期：YYYY-MM-DD
## 浏览器核心
| 功能 | 状态 | 首次加入 | 最近修改 | 说明 |
|---|---|---:|---:|---|
| Chromium 网页渲染 | ✅ 已完成 | v0.1.0 | v0.2.0 | 支持多进程和 GPU 加速 |
| 页面后退与前进 | ✅ 已完成 | v0.1.0 | v0.1.0 | 支持按钮和快捷键 |
| 开发者工具 | 🚧 开发中 | v0.3.0 | v0.3.0 | 当前仅开发版本可用 |
## 垂直标签页
| 功能 | 状态 | 首次加入 | 最近修改 | 说明 |
|---|---|---:|---:|---|
| 垂直标签列表 | ✅ 已完成 | v0.1.0 | v0.2.0 | 使用液态玻璃侧边栏 |
| 标签页拖动排序 | 🧪 测试中 | v0.2.0 | v0.2.0 | 正在测试跨分组拖动 |
| 标签页自动休眠 | 📋 已规划 | v0.4.0 | v0.4.0 | 根据内存压力自动处理 |
## 分屏浏览
| 功能 | 状态 | 首次加入 | 最近修改 | 说明 |
|---|---|---:|---:|---|
| 左右分屏 | ✅ 已完成 | v0.2.0 | v0.3.0 | 支持比例调整 |
| 上下分屏 | 🚧 开发中 | v0.3.0 | v0.3.0 | 基础布局已经完成 |
| 保存分屏组合 | 📋 已规划 | v0.4.0 | v0.4.0 | 保存页面和分屏比例 |
## 隐私保护
| 功能 | 状态 | 首次加入 | 最近修改 | 说明 |
|---|---|---:|---:|---|
| 追踪器拦截 | ✅ 已完成 | v0.2.0 | v0.3.0 | 支持站点例外 |
| 第三方 Cookie 限制 | ✅ 已完成 | v0.2.0 | v0.2.0 | 默认开启 |
| 指纹防护 | 🚧 开发中 | v0.3.0 | v0.3.0 | 部分网站存在兼容问题 |
## 液态玻璃界面
| 功能 | 状态 | 首次加入 | 最近修改 | 说明 |
|---|---|---:|---:|---|
| 液态玻璃侧边栏 | ✅ 已完成 | v0.1.0 | v0.2.0 | 支持深色和浅色模式 |
| 液态玻璃地址栏 | ✅ 已完成 | v0.1.0 | v0.2.0 | 支持降低透明度 |
| 液态玻璃分屏控制条 | 🧪 测试中 | v0.3.0 | v0.3.0 | 正在优化拖动性能 |

6. 单版本更新日志格式

CHANGELOG.md 按版本倒序排列，最新版本显示在最上方。

每个版本使用以下结构：

## v0.3.0 — YYYY-MM-DD
### 版本摘要
本版本重点完善分屏浏览、隐私盾牌和液态玻璃交互效果。
### 新增功能
- 新增上下分屏模式。
- 新增标签页拖拽创建分屏。
- 新增隐私盾牌站点报告。
- 新增分屏页面交换功能。
### 功能优化
- 优化垂直标签页拖动动画。
- 优化液态玻璃侧边栏的文字对比度。
- 优化分屏比例调整时的 Chromium 渲染性能。
- 降低后台标签页的内存占用。
### 问题修复
- 修复分屏状态下地址栏显示错误页面 URL 的问题。
- 修复关闭右侧页面后标签页状态未更新的问题。
- 修复深色模式下隐私盾牌文字对比度不足的问题。
- 修复浏览器恢复会话时标签页顺序错误的问题。
### 行为变化
- 标签页自动休眠默认时间由 30 分钟改为 60 分钟。
- 隐私窗口默认禁止扩展运行。
- 分屏中的页面不再参与自动休眠。
### 已移除功能
- 移除旧版顶部标签栏实验功能。
- 移除已废弃的分屏预览实现。
### 已知问题
- 部分 WebGL 页面调整分屏比例时可能短暂闪烁。
- 上下分屏暂不支持保存为收藏组合。
- 某些网站在严格指纹防护模式下可能无法正常登录。
### 正在开发
- 分屏组合保存。
- 跨窗口移动分屏。
- 工作空间独立 Cookie 容器。
- 内容拦截规则管理器。
### 升级提示
- 本版本会自动迁移旧版标签页数据。
- 升级后首次启动可能需要重新生成网站图标缓存。

7. 独立版本详情页

每个版本必须生成一个独立的可查看页面，例如：

Documentation/Releases/v0.3.0.md

详情页至少包含：

# v0.3.0 版本功能报告
发布日期：YYYY-MM-DD
构建编号：300
Chromium 版本：待填写
macOS 最低版本：待填写
发布通道：Alpha / Beta / Stable
## 本版目标
说明该版本主要解决的问题。
## 完成功能
列出本版本已完成的功能。
## 功能详情
说明每个功能的入口、使用方法和限制。
## 界面变化
记录液态玻璃 UI、标签页、分屏和设置界面的变化。
## 隐私变化
记录拦截规则、Cookie、权限和隐私窗口的变化。
## 性能变化
记录启动时间、内存占用、CPU 使用和分屏性能变化。
## 修复内容
记录已修复问题。
## 已知问题
记录当前仍然存在的问题。
## 测试情况
记录单元测试、UI 测试和兼容性测试结果。
## 升级与回退
说明升级注意事项、数据迁移方式和是否支持回退。

8. 软件内置功能日志页面

浏览器设置中必须提供用户可查看的“版本与功能”页面。

建议路径：

设置
└── 关于
    ├── 当前版本
    ├── 本版新功能
    ├── 完整功能列表
    ├── 已知问题
    └── 历史版本

软件内页面应支持：

* 查看当前版本号。
* 查看构建编号。
* 查看 Chromium 版本。
* 查看本版新增功能。
* 查看本版修复内容。
* 查看完整功能列表。
* 按模块筛选功能。
* 按完成状态筛选功能。
* 搜索功能名称。
* 查看历史版本。
* 展开查看功能详情。
* 复制版本信息。
* 导出诊断信息。

该页面同样使用液态玻璃 UI。

建议组件：

LiquidGlassReleaseNotesView
LiquidGlassFeatureListView
LiquidGlassVersionHistoryView
LiquidGlassFeatureStatusBadge
LiquidGlassReleaseDetailView

9. 软件内功能列表分类

软件内功能列表至少按以下模块展示：

* 浏览器核心。
* Chromium 集成。
* 垂直标签页。
* 标签页分组。
* 工作空间。
* 分屏浏览。
* 收藏夹。
* 历史记录。
* 下载管理。
* 隐私保护。
* 内容拦截。
* 网站权限。
* 隐私窗口。
* 液态玻璃 UI。
* 键盘快捷键。
* 触控板与拖拽。
* 性能优化。
* 无障碍。
* 安全。
* 开发者工具。

10. 结构化日志数据

除 Markdown 文件外，还需要生成可供软件读取的结构化日志：

Resources/ReleaseNotes/releases.json
Resources/ReleaseNotes/features.json

features.json 示例：

{
  "lastUpdatedVersion": "0.3.0",
  "categories": [
    {
      "id": "split-view",
      "name": "分屏浏览",
      "features": [
        {
          "id": "split-horizontal",
          "name": "左右分屏",
          "status": "completed",
          "introducedIn": "0.2.0",
          "updatedIn": "0.3.0",
          "description": "在同一窗口中并排显示两个网页",
          "supportsPrivateWindow": true,
          "supportsKeyboard": true,
          "limitations": []
        }
      ]
    }
  ]
}

releases.json 示例：

{
  "releases": [
    {
      "version": "0.3.0",
      "build": 300,
      "date": "YYYY-MM-DD",
      "channel": "beta",
      "summary": "完善分屏浏览和隐私保护",
      "added": [],
      "improved": [],
      "fixed": [],
      "changed": [],
      "removed": [],
      "knownIssues": [],
      "inProgress": []
    }
  ]
}

Markdown 日志和 JSON 日志必须来自同一数据源，避免内容不一致。

11. 自动生成要求

构建或发布流程必须自动检查版本日志。

每次版本发布前必须确认：

* 版本号已经更新。
* 构建编号已经更新。
* CHANGELOG.md 已更新。
* FEATURES.md 已更新。
* 独立版本详情页已生成。
* features.json 已更新。
* releases.json 已更新。
* 新增功能具有唯一功能 ID。
* 已完成功能具有测试状态。
* 已知问题已经记录。
* 被移除功能已明确标记。
* 软件内可以正常打开本版日志。

如果缺少日志或日志版本号与应用版本不一致，发布构建必须失败。

12. Git 提交与合并要求

每个功能分支或合并请求需要声明功能日志影响：

Feature Log Impact:
- Added:
- Improved:
- Fixed:
- Changed:
- Removed:
- Known Issues:

当提交包含用户可感知的变化时，必须更新功能日志。

以下改动通常需要写入日志：

* 新增功能。
* 修改交互。
* 修改默认设置。
* 修改隐私行为。
* 修改数据存储方式。
* 修改快捷键。
* 修改分屏行为。
* 修改标签页行为。
* 修改 UI。
* 性能明显变化。
* 修复用户可感知的问题。
* 移除或暂停功能。

以下内容可以不写入用户日志：

* 纯代码格式调整。
* 无行为变化的重构。
* 测试代码内部调整。
* 开发环境脚本小型修改。

但仍应保留在 Git 提交记录中。

13. 功能验收关联

每项功能应关联验收状态：

未开发
开发中
待测试
测试通过
测试失败
已发布
已废弃

功能日志可记录：

* 需求编号。
* 功能 ID。
* 开发负责人。
* 测试负责人。
* 单元测试状态。
* UI 测试状态。
* 发布版本。
* 已知限制。
* 回退方案。

14. 版本页面液态玻璃 UI

内置版本日志页面必须遵循液态玻璃设计系统。

设计要求：

* 左侧使用液态玻璃版本导航栏。
* 右侧显示版本详情。
* 当前版本使用明显但克制的高光状态。
* 功能状态使用文字、图标和颜色共同表达。
* 支持深色和浅色模式。
* 支持降低透明度。
* 支持增强对比度。
* 支持 VoiceOver。
* 支持键盘选择版本和展开功能。
* 长列表使用懒加载。
* 不使用过度动画。

15. 最终交付补充要求

最终交付内容中增加：

1. CHANGELOG.md 模板。
2. FEATURES.md 模板。
3. ROADMAP.md 模板。
4. 版本详情页模板。
5. features.json 数据结构。
6. releases.json 数据结构。
7. Swift 日志数据模型。
8. 日志解析服务。
9. 软件内功能列表页面。
10. 软件内版本历史页面。
11. 液态玻璃版本详情组件。
12. 构建阶段日志校验脚本。
13. 版本发布自动生成流程。
14. 功能状态与测试状态同步方案。

每完成一个开发版本，必须先更新功能列表日志，才能将该版本标记为可发布。

最终产品应吸收 Arc 在垂直标签页和分屏浏览方面的优秀设计思想，以及 Brave 在隐私保护方面的产品理念，但必须建立独立的产品名称、视觉系统、功能命名、交互细节和技术实现，不得进行像素级复制。