# Rex v0.9.5

发布日期：2026-07-28<br>
构建号：952<br>
通道：Beta<br>
架构：Apple Silicon（arm64）<br>
内核：Chromium 150.0.7871.129 / CEF 150.0.14

## 概要

v0.9.5 build 952 修复网页因错误自动化指纹、系统代理被绕过和启动导航竞态而永久白屏的问题；隐私盾牌恢复 Safari 版本的按网站设置方式，扩展入口则改为更醒目的 Rex 高对比度面板，同时保留扩展自身 popup 的行为。

## 主要变化

### 网页加载

- 内部扩展管理继续使用无监听端口的 DevTools pipe，但页面不再暴露 `navigator.webdriver=true`，User-Agent 也恢复 Chromium 标准 Chrome 版本段。
- CEF 不再强制追加 `--no-proxy-server`，会遵循 macOS 系统代理。
- 启动阶段的地址提交会等待页面创建和站点隐私策略应用完成后再执行，首次导航不再被丢弃。
- 扩展启动对账失败仍会报告错误，但不再永久阻塞网页；没有后续补偿事务时会释放等待中的导航。
- 真实应用已验证 `https://itdog.cn/` 可以完成反自动化校验并显示网站正文。

### 隐私盾牌

- 新增按 profile/host 唯一持久化的 `SitePrivacyPolicy`。
- 保护开关与级别立即同步所有同站标签，重启后继续复用。
- 网站选择不再修改工作空间默认级别；进入其他网站时应用其自身策略或原默认值。
- 隐私窗口只在内存中应用，不写入普通 profile。

### 扩展面板

- 静态 popup 的 `chrome.tabs.query({active:true,currentWindow:true})` 返回点击扩展按钮前的真实 HTTP(S) 来源标签。
- 在 `rex-extension://` 页面重新打开扩展时沿用最近有效网页来源，使扩展设置仍作用于目标网站。
- 工具栏扩展列表与 popup 改为独立 AppKit 浮窗，不再通过 SwiftUI `.popover` 和 `NSWindow.addChildWindow` 附着到 CEF 远程视图。
- 面板尺寸与关闭回调绑定当前会话 ID，旧回调不会关闭或改变后来打开的面板。
- 工具栏扩展列表使用深靛蓝实体渐变、清晰描边与高对比度文字，在浅色网页上方也能明确区分；扩展自身的 Chromium popup 保持原样。

## 验证

- Swift Testing：`144/144` 通过。
- CEF bridge arm64 Release：通过。
- Xcode Release / 完整 Chromium App：通过。
- Release Notes Validator：v0.9.5 build 952，28 个功能 ID。
- 主 App、CEF 与五个 Helper 均为 arm64；主 App 与五个 Helper 均为 `0.9.5 / 952`。
- App 与复制后的交付包均通过 ad-hoc deep/strict codesign；ZIP 完整解压测试和 SHA-256 清单校验通过。
- 2026-07-29 预发布 DMG 通过 CRC、只读挂载、内容结构、App 版本、arm64 和 deep/strict codesign 验证。
- 隔离 Home 注入缺失 content script 后，Chromium 连续拒绝启动 generation 1/2；Rex 保留扩展错误并记录 generation 2 失败后释放网页导航屏障，主窗口仍可继续提交新导航。

## 已知边界

- Rex 小型面板不是 Chromium 原生 action popup。v0.9.5 只为静态 popup 提供受控来源标签查询上下文，完整 `activeTab` 权限授予仍不支持。
- 未声明静态 `default_popup` 时的 `action.onClicked`、运行时 `action.setPopup` 动态变更仍不支持。
- 网站策略按精确主机名保存，尚未按完整 PSL/eTLD+1 合并子域。
- Developer ID 签名、公证与自动更新尚未完成。

## 构建

```bash
Scripts/package-chromium-app.sh 0.9.5 952 Release
```

## 产物

- App：[Dist/Rex.app](../../Dist/Rex.app)，`342M`（`350588 KiB`）。
- ZIP：[Dist/Rex-v0.9.5-macos-arm64-chromium.zip](../../Dist/Rex-v0.9.5-macos-arm64-chromium.zip)，`142,539,578` bytes（`147576 KiB` / `144M`）。
- ZIP SHA-256：`380442d0413b084df39ccd318c6c4e9e4c64bd6fd18edcd17f29bd1858025b20`。
- 预发布 DMG：[Dist/Rex-v0.9.5-build952-macos-arm64-prerelease.dmg](../../Dist/Rex-v0.9.5-build952-macos-arm64-prerelease.dmg)，`160,241,220` bytes（`156488 KiB` / `153M`）。
- DMG SHA-256：`8ccb78088fadba83a0eabd6e983494e5213ed54bf137bb19aca572c745a13d4b`。
- 构建清单：[Dist/PACKAGE-INFO.txt](../../Dist/PACKAGE-INFO.txt)；DMG 清单：[Dist/DMG-INFO.txt](../../Dist/DMG-INFO.txt)；校验清单：[Dist/SHA256SUMS](../../Dist/SHA256SUMS)。
